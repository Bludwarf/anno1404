import { UtilizationPipe } from './utilization.pipe';

describe('UtilizationPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilizationPipe();
    expect(pipe).toBeTruthy();
  });
});
